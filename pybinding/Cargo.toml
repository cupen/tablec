[package]
name = "pybinding"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
path = "tablec/lib.rs"

[dependencies]
tablec = { path = ".." }
pyo3 = { version = "0.22.0", features = ["auto-initialize", "extension-module", "multiple-pymethods"] }
